# Branch Index

`main` = baseline only (never receives feature merges).
Slice branches merge into feature integration branches with `--no-ff`.

| # | Slice branch | Layer | Scope | Merged into |
|---|--------------|-------|-------|-------------|

`main` holds project baseline only.
